[project]
name = "cc-model-switch"
version = "1.1.1"
description = "Edit models in Claude Code profile more easily."
requires-python = ">=3.12"
readme="README.md"
dependencies = []

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
