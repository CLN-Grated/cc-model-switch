[project]
name = "cc-model-switch"
version = "1.6.0"
description = "Edit models in Claude Code profile more easily."
requires-python = ">=3.12"
readme="README.md"
dependencies = []

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
